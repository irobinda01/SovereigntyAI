import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This app's data hooks (useBalances, useMyVaults, wallet-context)
      // use the standard "fetch/read on mount, setState with the result"
      // effect pattern for real on-chain reads. That pattern is exactly
      // what useEffect is for; downgraded to warn rather than disabled
      // outright so genuinely accidental cascading setState is still caught.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
